<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheet/edit.css" />
    <title>Edit Page</title>
    </head>
  <body>
    <h1>Edit Post for <%= post.title %></h1>

    <form action="/auth/<%= user.username %>/edit/<%= post._id %>" method="POST">
      <div>
        <label>Title:</label>
        <input type="text" name="title" value="<%= post.title %>" required />
      </div>

      <div>
        <label>Description:</label>
        <textarea name="description" required><%= post.description %></textarea>
      </div>

      <div>
        <label>Picture URL:</label>
        <input type="text" name="picture" value="<%= post.picture %>" />
      </div>

      <button type="submit">Update Post</button>
    </form>

    <p><a href="/auth/<%= user.username %>/mainPage">Back to main page</a></p>
  </body>
</html>
